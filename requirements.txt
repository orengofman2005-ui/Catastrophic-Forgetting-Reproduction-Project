# requirements.txt
# Catastrophic Forgetting — PyTorch Reproduction
# Tested on Python 3.11 / PyTorch 2.1.0+cu118 / CUDA 11.8
#
# GPU install (CUDA 11.8) — recommended:
#   pip install -r requirements.txt
#
# CPU-only fallback (replaces torch lines below):
#   pip install torch==2.1.0 torchvision==0.16.0 numpy scikit-learn matplotlib tqdm

--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.1.0+cu118
torchvision==0.16.0+cu118
torchaudio==2.1.0+cu118
numpy>=1.24.0
scikit-learn>=1.2.0
matplotlib>=3.7.0
tqdm>=4.65.0
