# requirements.txt
# Catastrophic Forgetting — PyTorch Reproduction
# Tested on Python 3.11 (required: Python >= 3.9)
#
# Install with:  pip install -r requirements.txt

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
scikit-learn>=1.2.0
matplotlib>=3.7.0
tqdm>=4.65.0
